# Database
DB_PASSWORD=changeme

# GitHub (optional — raises API rate limit from 60 to 5000 req/hour)
GITHUB_TOKEN=

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Web Push VAPID (generated automatically on first run if empty)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com

# Backend
PORT=3000
NODE_ENV=production
